# KAMAI – Next.js Starter

This is a minimal Next.js (App Router) + Tailwind starter ready for Vercel.

## Commands
```bash
npm install
npm run dev
```
Then open http://localhost:3000

Deploying on Vercel: push to `main` and Vercel will build automatically.
